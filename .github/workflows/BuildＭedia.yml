name: Build Media3 & Release AAR

# ======================= 【可修改变量】 =======================
env:
  REPO_URL: FongMi/media  # 仓库地址
# ==============================================================

on:
  workflow_dispatch:  # 手动触发
    inputs:
      target_branch:
        description: '输入要编译的分支名'
        required: true
        default: 'release-1.10.1-fongmi'
        type: string
      copy_to_oktv:
        description: '是否更新源仓库'
        required: true
        default: false
        type: boolean

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          repository: ${{ env.REPO_URL }}
          ref: ${{ inputs.target_branch }}
          fetch-depth: 1

      # ====================== 替换 Gradle 下载源为腾讯云 ======================
      - name: Replace Gradle Distribution URL with Tencent Mirror
        run: |
          GRADLE_PROPS="gradle/wrapper/gradle-wrapper.properties"
          if [ -f "$GRADLE_PROPS" ]; then
            sed -i 's|services.gradle.org/distributions|mirrors.cloud.tencent.com/gradle|g' "$GRADLE_PROPS"
            echo "✅ 已替换 Gradle 下载源为腾讯云镜像"
            cat "$GRADLE_PROPS"
          else
            echo "⚠️ 未找到 gradle-wrapper.properties 文件，跳过替换"
          fi
      # ====================================================================

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: gradle

      - name: Grant execute permission
        run: chmod +x gradlew

      - name: Build Release AAR
        run: ./gradlew assembleRelease
        
      - name: Get current date
        id: date
        run: echo "datetime=$(date +%Y%m%d%H%M%S%3N)" >> $GITHUB_OUTPUT

      # 只收集 move.txt 中列出的 AAR
      - name: Collect only AAR in move.txt
        run: |
          mkdir -p release_aars
          while IFS= read -r aar_name || [ -n "$aar_name" ]; do
            aar_name=$(echo "$aar_name" | xargs)
            if [ -z "$aar_name" ]; then continue; fi
            find ./libraries -name "$aar_name" -type f -exec cp {} release_aars/ \;
          done < move.txt

      # 复制 AAR 到 ok-tv 仓库
      - name: Clone → Copy AAR → Commit → Push to ok-tv
        if: ${{ inputs.copy_to_oktv == true }}
        run: |
          cd ..
          git clone -b fm-new https://cyh92:${{ secrets.MY_GITEE_TOKEN }}@gitee.com/cyh92/ok-tv.git ok-tv-repo
          cd ok-tv-repo
          
          # 复制AAR
          cp -f ${{ github.workspace }}/release_aars/*.aar app/libs/
          
          # 配置Git
          git config user.name "GitHub Action"
          git config user.email "action@github.com"
          
          # 提交变更
          git add app/libs/*.aar
          git commit -m "Auto update Media3 AARs (${{ steps.date.outputs.datetime }})"
          
          # 推送到远程fm-new分支
          git push origin fm-new
          
          echo "✅ AAR复制 + 提交 + 推送全部完成！"

      # 打包 ZIP
      - name: Zip all AARs
        run: |
          cd release_aars
          zip -q -r Media3-AARs.zip *.aar
          mv Media3-AARs.zip ../

      # 创建 Release 并上传
      - name: Create Release and Upload All
        uses: softprops/action-gh-release@v2
        with:
          tag_name: build-Media3-${{ steps.date.outputs.datetime }}
          name: Release AAR Media3-${{ steps.date.outputs.datetime }}
          files: |
            Media3-AARs.zip
            release_aars/*.aar
